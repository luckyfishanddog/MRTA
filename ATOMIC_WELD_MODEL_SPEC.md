# 固定原子焊缝模型规范

版本：`atomic_weld_partition_routing_v1`；日期：2026-07-16。

## 1. 目的与适用范围

本模型把几何切分从搜索过程中移到确定性预处理阶段。搜索算法只能读取固定原子焊缝集合、离散边界事件和父子映射，不能新增、删除、移动或再次切分焊缝。旧 `mrta_problem_core.py` 的动态切分模型保持不变，仅供旧冻结算法使用。

## 2. 确定性预处理

对每个冻结 selected XLSX，按稳定原焊缝 ID 排序后执行：

1. 若线段严格跨越 `y=6 m`，在交点处精确切为上、下半区两段；不跨越则按中点的确定性规则归入一个半区。
2. 在每个半区片段上计算水平投影 `Lx=|x2-x1|`。
3. `Lx<=5 m` 时不切分；`Lx>5 m` 时令 `k=max(2,floor(Lx/1 m))`，沿原参数方向等分为 `k` 段。
4. 每段必须满足 `Lx/k>=1 m` 且不超过 5 m；例如 6.6 m 得到 6×1.1 m。
5. 保存稳定 `atomic_weld_id`、完整父映射与源实例哈希。

原子字段包括：`atomic_weld_id`、`parent_weld_id`、`parent_original_id`、`half_region`、`segment_index/count`、`start/end`、欧氏/水平长度、焊接时间、两类切分标记和 `source_instance_hash`。

实际结果：w30 为 30→31（y6 原子段 2，长切分 0）；w45 为 45→48（6，0）；w60 为 60→74（6，12）。w60 的 12.5 m 水平焊缝被等分为 12×1.0416666667 m。

## 3. 离散边界事件

上下半区分别从平台边界和全部原子端点 x 坐标构造点单元与开区间单元。候选代表点不得满足任一原子焊缝的 `xmin < b < xmax`。对每个可行单元计算左右任务集合并以其 SHA-256 作为赋值身份；相同赋值只保留一个代表。

事件保存索引、代表 x、等价区间、左右原子 ID 集合和赋值哈希。测试在代表点及区间 1/4、3/4 位置复算赋值，证明区间内恒定。事件数：w30 上/下 11/5，w45 10/8，w60 11/7。

## 4. 路线、方向与零空移

路线开放，不计初始定位和返回；setup/post 均为 0。焊缝双向执行，固定顺序的方向由二状态 DP 在 O(m) 内精确求解。仅预建一次全局 `4N²` 有向端点转移表。

若前段终点与后段起点的三维欧氏距离不超过 `1e-9 m`，该方向转移的空移时间和距离严格为 0；否则沿用公共三阶段空移模型。实现同时预计算零后继和同父相邻段。

## 5. 目标与归一化

系统指标为 makespan、四机器人总时间极差、空移距离总和。权重固定为 `[0.7,0.2,0.1]`。新模型重新构造 `ideal_baseline_range_v1`：理论理想点为纯焊接总时间四等分、零不均衡、零空移；确定性基线使用工作量平衡事件和 cheapest insertion。新规范哈希为 `031cfa7c8fe1668e38c463f6142807d09a3f159d6c627f2cf8d5164c4aaf4dcb`。

## 6. 不变量

- 每个原子只属于一个半区；每个事件把该半区所有原子恰好分配一次。
- 每个系统解覆盖全部原子且无重复。
- 搜索期切分为 false；方向 DP、路线评价和系统聚合可独立重算。
- 原子实例、归一化和候选解均以哈希交叉验证。

