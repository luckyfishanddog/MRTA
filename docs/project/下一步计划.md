# 下一步计划：四算法公平实验协议

当前正式算法为 GA+ACO、DE-LKH-style、Paper-Aligned-HGA-XCut-Control 和 ABMA。历史原型，现已删除，不属于当前代码基线。

正式 HGA 启动命令：

```powershell
D:\pybullet_test\.venv\Scripts\python.exe `
  MRTA_HGA_PAPER_ALIGNED_CONTROL.py `
  --config-path paper_aligned_hga_control_defaults.json `
  --instance-path data\instances\selected\instance_w30.xlsx `
  --solver-seed 42
```

正式长实验前仍需确认：

1. 四算法统一预算和停止口径；
2. 统一 seed 列表与重复次数；
3. 相同物理参数、目标权重和 refs；
4. 先运行 w30 多 seed 并保存逐 seed CSV/JSON/log；
5. 通过 w30 审计后再决定 w45/w60；
6. collision audit 保持独立，不混入主目标。

本文件不授权启动正式实验。
